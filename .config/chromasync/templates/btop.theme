@out: ~/.config/btop/themes/omnichroma.theme
@fmt: #hex

# Main background, empty for terminal default, need to be empty if you want transparent background
theme[main_bg]="{BG}"

# Main text color
theme[main_fg]="{FG}"

# Title color for boxes
theme[title]="{FG}"

# Highlight color for keyboard shortcuts
theme[hi_fg]="{OS1}"

# Background color of selected item in processes box
theme[selected_bg]="{FG}"

# Foreground color of selected item in processes box
theme[selected_fg]="{BG}"

# Color of inactive/disabled text
theme[inactive_fg]="{BG:70:FG}"

# Color of text appearing on top of graphs, i.e uptime and current network graph scaling
theme[graph_text]="{FG}"

# Background color of the percentage meters
theme[meter_bg]="{BG:80:FG}"

# Misc colors for processes box including mini cpu graphs, details memory graph and details status text
theme[proc_misc]="{FG}"

# CPU, Memory, Network, Proc box outline colors
theme[cpu_box]="{GRN:100:BG}" #Mauve
theme[mem_box]="{BLU:100:BG}" #Green
theme[proc_box]="{CYN:100:BG}" #Blue
theme[net_box]="{MAG:100:BG}" #Maroon

# Box divider line and small boxes line color
theme[div_line]="{BG:80:FG}"

# Temperature graph color
theme[temp_start]="{GRN}"
theme[temp_mid]="{GRNH}"
theme[temp_end]="{RED}"

# CPU graph colors
theme[cpu_start]="{GRN}"
theme[cpu_mid]="{GRNH}"
theme[cpu_end]="{RED}"

# Mem/Disk free meter
theme[free_start]="{BLU}"
theme[free_mid]="{BLUH}"
theme[free_end]="{RED}"

# Mem/Disk cached meter
theme[cached_start]="{BLU}"
theme[cached_mid]="{BLUH}"
theme[cached_end]="{RED}"

# Mem/Disk available meter
theme[available_start]="{BLU}"
theme[available_mid]="{BLUH}"
theme[available_end]="{RED}"

# Mem/Disk used meter
theme[used_start]="{BLU}"
theme[used_mid]="{BLUH}"
theme[used_end]="{GRN}"

# Download graph colors
theme[download_start]="{MAG}"
theme[download_mid]="{MAGH}"
theme[download_end]="{RED}"

# Upload graph colors
theme[upload_start]="{MAG}"
theme[upload_mid]="{MAGH}"
theme[upload_end]="{RED}"

# Process box color gradient for threads, mem and cpu usage
theme[process_start]="{CYN}"
theme[process_mid]="{CYNH}"
theme[process_end]="{RED}"
